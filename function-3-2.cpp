#include <iostream>

int median_array(int array[], int n) {
    if (n <= 1 || n % 2 == 0) {
        return 0;
    }
    
    int min = array[0];
    for (int i = 1; i < n; i++) {
        if (array[i] < min) {
            min = array[i];
        }
        return min;

    int sorted_array[]
    
    int sorted_array[i] = int min;

    for (i = min)
    }
}